# Telebit APT repository

Served as a static APT repo for [telebit](https://github.com/sonnam0904/telebit)
(Vietnamese Telex/VNI input method for fcitx5). Populated automatically by the
`publish-apt` job in `.github/workflows/release.yml` on each release — do not
edit files here by hand.
